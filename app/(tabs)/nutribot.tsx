/**
 * NutriBot Tab Screen — AI health assistant chat.
 *
 * Re-exports the NutriBot component from the root-level screen.
 * Back button only shown when navigated with a conversationId param.
 */

export { default } from '../nutribot';
